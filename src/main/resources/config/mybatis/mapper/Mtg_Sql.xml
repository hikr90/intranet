<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mtg">
	<select id="mtgInqyDao1010" resultType="CamelHashMap" parameterType="java.util.HashMap">
		/* mtgInqyDao1010 : 회의 시퀀스 조회 */
		SELECT 'MTG_' || LPAD(NVL(MAX(REPLACE(MTG_CD,'MTG_')),0) + 1,4,0) AS SEQUENCE_ID FROM MEETING
	</select>
	
	<select id="mtgInqyDao1020" resultType="CamelHashMap" parameterType="java.util.HashMap">
		/* mtgInqyDao1020 : 회의 목록 조회 */
		SELECT 
	   			M.MTG_CD
	   		,	M.MTG_CD AS ID	
	   		,	M.MTG_TITLE AS TITLE
			,	TO_CHAR(TO_DATE(M.MTG_DT,'YYYY-MM-DD'), 'YYYY-MM-DD') || ' 00:00:00' AS "START"
			,	TO_CHAR(TO_DATE(M.MTG_DT,'YYYY-MM-DD'), 'YYYY-MM-DD') || ' 00:00:00' AS "END"
	   		,	M.MTG_DT
	   		,	M.MTG_STM
	   		,	M.MTG_ETM
	   		,	M.MTG_IDX
	   		,	E.EMP_NM
	   		,   O.ORG_NM
	   		,	R.RANK_NM
	        ,   M.REG_DT
	        ,   M.REG_TM
	        ,	M.MTG_LOC
	        ,	CC_LOC.COMMCODE_NM AS MTG_LOC_NM
		FROM MEETING M
	    LEFT OUTER JOIN EMP_INFO E
			ON M.MTG_IDX = E.EMP_IDX
		LEFT OUTER JOIN ORG O
        	ON O.ORG_CD = E.ORG_CD
            AND O.USE_YN = 'Y'
		LEFT OUTER JOIN RANK R
			ON R.RANK_CD = E.RANK_CD
            AND R.USE_YN = 'Y'
        LEFT OUTER JOIN COMMCODE CC_LOC
			ON CC_LOC.COMMCODE_CD = M.MTG_LOC
		WHERE M.USE_YN = 'Y'
        	<if test="srchNm != null and srchNm != ''">
         		AND REGEXP_REPLACE(LOWER(M.MTG_TITLE),'[^0-9가-힣A-Za-z]','') LIKE '%'||REGEXP_REPLACE(LOWER(#{srchNm, jdbcType=VARCHAR}),'[^0-9가-힣A-Za-z]','')||'%'
         	</if>
			<if test="orgNm != null and orgNm != ''">
				AND REGEXP_REPLACE(LOWER(O.ORG_NM),'[^0-9가-힣A-Za-z]','') LIKE '%'||REGEXP_REPLACE(LOWER(#{orgNm, jdbcType=VARCHAR}),'[^0-9가-힣A-Za-z]','')||'%'
			</if>
			<if test="rankNm != null and rankNm != ''">
         		AND REGEXP_REPLACE(LOWER(R.RANK_NM),'[^0-9가-힣A-Za-z]','') LIKE '%'||REGEXP_REPLACE(LOWER(#{rankNm, jdbcType=VARCHAR}),'[^0-9가-힣A-Za-z]','')||'%'
			</if>
			<if test="srchLocCd != null and srchLocCd != ''">
         		AND M.MTG_LOC = #{srchLocCd, jdbcType=VARCHAR}
         	</if>
	</select>

	<select id="mtgInqyDao1030" resultType="CamelHashMap" parameterType="java.util.HashMap">
		/* mtgInqyDao1030 : 회의 상세 조회 */
		SELECT
				DATA.*
		FROM (
					SELECT 
								M.MTG_CD
							,	M.MTG_TITLE
							,	M.MTG_CONT
							,	M.MTG_IDX
							,	M.MTG_DT
							,	M.MTG_STM
							,	M.MTG_ETM
							,	M.MTG_LOC
							,	CC_LOC.COMMCODE_NM AS MTG_LOC_NM
							,	E.ORG_CD
							,	O.ORG_NM
							,	E.EMP_NM
							,	E.RANK_CD
							,	R.RANK_NM
					FROM MEETING M
					LEFT OUTER JOIN EMP_INFO E
						ON M.MTG_IDX = E.EMP_IDX
					LEFT OUTER JOIN ORG O
						ON O.ORG_CD = E.ORG_CD
						AND O.USE_YN = 'Y'
					LEFT OUTER JOIN RANK R
						ON R.RANK_CD = E.RANK_CD
						AND R.USE_YN = 'Y'
					LEFT OUTER JOIN COMMCODE CC_LOC
						ON CC_LOC.COMMCODE_CD = M.MTG_LOC
					WHERE M.MTG_CD = #{sequenceId, jdbcType=VARCHAR}
		) DATA
	</select>
	
	<select id="mtgInqyDao1040" resultType="CamelHashMap" parameterType="java.util.HashMap">
		/* mtgInqyDao1040 : 회의 중복 조회 */
		SELECT 
				MTG_CD
			,	MTG_STM
			, MTG_ETM
		FROM MEETING
		WHERE 1=1
			<if test="sequenceId != null and sequenceId != ''">
				AND MTG_CD != #{sequenceId, jdbcType=VARCHAR}
			</if>
			
			<!-- MTG_DT -->
			<choose>
				<when test="mtgDt != null and mtgDt != ''">
					AND MTG_DT = REPLACE(#{mtgDt, jdbcType=VARCHAR},'-', '')
				</when>
				<otherwise>
					AND MTG_DT = TO_CHAR(SYSDATE, 'YYYYMMDD')
				</otherwise>
			</choose>
			
			<!-- MTG_LOC -->
			<choose>
				<when test="mtgLoc != null and mtgLoc != ''">
					AND MTG_LOC = #{mtgLoc, jdbcType=VARCHAR}
				</when>
				<otherwise>
					AND MTG_LOC = 'LOC_0010'
				</otherwise>
			</choose>
	</select>
	
	<select id="mtgInqyDao2010" resultType="CamelHashMap" parameterType="java.util.HashMap">
		/* mtgInqyDao2010 : 회의 (캘린더) 조회 */
		SELECT 
				'' AS "TITLE"
			,	TO_CHAR(TO_DATE(M.MTG_DT,'YYYY-MM-DD'), 'YYYY-MM-DD') || ' 00:00:00' AS "START"
			,	TO_CHAR(TO_DATE(M.MTG_DT,'YYYY-MM-DD'), 'YYYY-MM-DD') || ' 00:00:00' AS "END"
		FROM MEETING M
		GROUP BY M.MTG_DT
	</select>
	
	<select id="mtgInqyDao2020" resultType="CamelHashMap" parameterType="java.util.HashMap">
		/* mtgInqyDao2020 : 캘린더 목록 조회 */
		SELECT
				M.MTG_CD
			,	M.MTG_TITLE
			,	M.MTG_CONT
			,	M.MTG_IDX
			,	M.MTG_DT
			,	E.EMP_NM
			,	O.ORG_NM
			,	R.RANK_NM
			,	M.MTG_STM
			,	M.MTG_ETM
		FROM MEETING M
		LEFT OUTER JOIN EMP_INFO E
			ON M.MTG_IDX = E.EMP_IDX
		LEFT OUTER JOIN ORG O
			ON O.ORG_CD = E.ORG_CD
		LEFT OUTER JOIN RANK R
			ON R.RANK_CD = E.RANK_CD
		WHERE 1=1
			<choose>
				<when test="mtgDt != null and mtgDt != ''">
					AND M.MTG_DT = #{mtgDt, jdbcType=VARCHAR}
				</when>
				<otherwise>
					AND M.MTG_DT = TO_CHAR(SYSDATE,'YYYYMMDD')
				</otherwise>
			</choose>
	</select>
	
	<insert id="mtgProcDao1010" parameterType="java.util.HashMap">
		/* mtgProcDao1010 : 회의 등록 처리 */
		INSERT INTO MEETING
		(
				MTG_CD
			,	MTG_TITLE
			,	MTG_CONT
			,	MTG_IDX
			,	MTG_DT
			,	MTG_STM
			,	MTG_ETM
			,	MTG_LOC
			,	REG_DT
			,	REG_TM
			,	USE_YN
			,	DISP_ORDER
			,	REMARK
		)
		VALUES
		(
				#{sequenceId, jdbcType=VARCHAR}
			,	#{mtgTitle, jdbcType=VARCHAR}
			,	#{mtgCont, jdbcType=VARCHAR}
			,	#{empIdx, jdbcType=VARCHAR}
			,	REPLACE(#{mtgDt, jdbcType=VARCHAR},'-', '')
			,	REPLACE(#{mtgStm, jdbcType=VARCHAR},':','')
			,	REPLACE(#{mtgEtm, jdbcType=VARCHAR},':','')
			,	#{mtgLoc, jdbcType=VARCHAR}
			,	TO_CHAR(SYSDATE,'YYYYMMDD')
			,	TO_CHAR(SYSDATE,'HH24MISS')
			,	'Y'
			,	(
					SELECT 
						NVL(MAX(DISP_ORDER),0) + 1 
					FROM MEETING
				)
			,	NULL
			)
	</insert>
	
	<update id="mtgProcDao1020" parameterType="java.util.HashMap">
		/* mtgProcDao1020 : 회의 삭제 처리 (수정)  */
		DELETE MEETING
		WHERE MTG_CD = #{sequenceId, jdbcType=VARCHAR}
	</update>
	
	<update id="mtgProcDao1030" parameterType="java.util.HashMap">
		/* mtgProcDao1030 : 회의 수정 처리 */
		UPDATE MEETING
			SET 	MTG_TITLE = #{mtgTitle, jdbcType=VARCHAR}
				,	MTG_CONT = #{mtgCont, jdbcType=VARCHAR}
				,	MTG_IDX = #{empIdx, jdbcType=VARCHAR}
				,	MTG_DT = REPLACE(#{mtgDt, jdbcType=VARCHAR},'-','')
				,	MTG_STM = REPLACE(#{mtgStm, jdbcType=VARCHAR},':','')
				,	MTG_ETM = REPLACE(#{mtgEtm, jdbcType=VARCHAR},':','')
				,	MTG_LOC = #{mtgLoc, jdbcType=VARCHAR}
		WHERE MTG_CD = #{sequenceId, jdbcType=VARCHAR} 
	</update>	
</mapper>